mixin boxRow(boxes)
    div(class="d-md-flex flex-md-equal w-100 my-md-3 pl-md-3")
        each box in boxes
            - var backgroundColor = (box === "dark" ? "bg-dark" : "bg-light")
            - var textColor = (box === "dark" ? "text-white" : "text-dark")
            - var imageColor = (box === "dark" ? 'bg-light' : 'bg-dark')
                div(class=`${backgroundColor} ${textColor} mr-md-3 pt-3 px-3 pt-md-5 px-md-5 text-center overflow-hidden`)
                    div(class="my-3 py-3")
                        h2(class="display-5") Another headline
                        p(class="lead") And an even wittier subheading.
                    div(class=`${imageColor} box-shadow mx-auto`, style="width: 80%; height: 300px; border-radius: 21px 21px 0 0;")
